<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>home - Laika Framework</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Favicon -->
    <link href="{{ 'assets/img/icon.png' | asset }}" rel="icon" sizes="32x32">

    {{ 'lf_header' | hook }}
</head>
<body>
    <div class="container">
        <img class="logo" src="{{ 'assets/img/logo.png' | asset }}">
        <h1>{{ welcome }}</h1>
        <p>PHP MVC Framework for Fast, Clean & Rest API Development.</p>
        <div class="footer">
            <div class="copyright">&copy; {{ '' | date('Y') }}. {{ 'app_name' | hook }}</div>
        <div>
    </div>
    {{ 'lf_footer' | hook }}
</body>
</html>
